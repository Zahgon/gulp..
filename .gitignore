# Compiled binaries
# Root-anchored so the tracked `bin/gulp` launcher is not ignored.
/gulp
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test and coverage output (see `make test` / `make cover`)
coverage.out
*.test

# Go workspace files
go.work
go.work.sum

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
